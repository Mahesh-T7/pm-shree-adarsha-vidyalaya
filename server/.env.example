# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Connection (SQLite locally, PostgreSQL in production)
# PostgreSQL example: postgresql://user:password@localhost:5432/pmshri_school?schema=public
DATABASE_URL="file:./dev.db"

# Authentication Secrets (Generate secure 64-byte random strings in production)
JWT_SECRET=pmshri_adarsha_super_secret_jwt_key_2026_change_in_production
JWT_EXPIRES_IN=1h
REFRESH_SECRET=pmshri_adarsha_super_secret_refresh_key_2026_change_in_production
REFRESH_EXPIRES_IN=7d
COOKIE_SECRET=pmshri_cookie_signing_secret_2026

# Admin Bootstrap Credentials (Used only for first seed)
INITIAL_ADMIN_USERNAME=headmaster
INITIAL_ADMIN_PASSWORD=Adarsha@Sindhanur2026!
INITIAL_ADMIN_EMAIL=principal@pmshreeadarshasindhanur.org
